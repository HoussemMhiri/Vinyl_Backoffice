{
  "kind": "collectionType",
  "collectionName": "channel_listings",
  "info": {
    "singularName": "channel-listing",
    "pluralName": "channel-listings",
    "displayName": "Channel Listing",
    "description": "State of one sellable unit published on one marketplace channel"
  },
  "options": {
    "draftAndPublish": false
  },
  "attributes": {
    "tenant": {
      "type": "relation",
      "relation": "manyToOne",
      "target": "api::tenant.tenant",
      "required": true
    },
    "sellableUnit": {
      "type": "relation",
      "relation": "manyToOne",
      "target": "api::sellable-unit.sellable-unit",
      "inversedBy": "channelListings",
      "required": true
    },
    "channel": {
      "type": "enumeration",
      "enum": ["discogs"],
      "default": "discogs",
      "required": true
    },
    "listingStatus": {
      "type": "enumeration",
      "enum": ["not_published", "pending", "published", "failed", "removed", "sync_error"],
      "default": "not_published",
      "required": true
    },
    "externalListingId": {
      "type": "string"
    },
    "externalUrl": {
      "type": "string"
    },
    "publishedPrice": {
      "type": "decimal",
      "min": 0
    },
    "lastSyncedAt": {
      "type": "datetime"
    },
    "lastErrorMessage": {
      "type": "text"
    }
  }
}
